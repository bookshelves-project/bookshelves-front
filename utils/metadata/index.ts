// import dynamicMeta from './dynamic'
// import staticMeta from './static'

const config = {
  // static: () => staticMeta(),
  // dynamic: (meta: any, baseURL?: string) => dynamicMeta(meta, baseURL),
}

export default config
